#include <stdio.h>
#include <stdlib.h>
int main(){
    //Acesso aos elementos pelo índice
    float notas[100];
    notas[0] = 81; //primeiro índice
    notas[99] = 72; //último índice
    system("pause");
    return 0;
}

